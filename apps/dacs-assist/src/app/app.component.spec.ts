import { TestBed } from '@angular/core/testing'; import { provideZonelessChangeDetection } from '@angular/core'; import { provideHttpClient } from '@angular/common/http'; import { of } from 'rxjs'; import { AppComponent } from './app.component'; import { CockpitFacade } from './features/production-cockpit/cockpit.facade';
describe('AppComponent',()=>{it('transitions from loading to a rendered recommendation',async()=>{const recommendation={wardId:'224',wardName:'Cardiologie',patientCount:2,totalOrders:2,producedOrders:1,totalDoses:20,producedDoses:10,progressPercent:50,medianRcp:.2,rcpCoveragePercent:100,cutoff:null,estimatedDurationMinutes:null,exceptionCount:0,blocked:false,rationaleCodes:[]};const facade={load:()=>of({wards:[recommendation],recommendation,totalDoses:20,producedDoses:10,patients:2,globalCutoff:null,productionStart:null,origin:'MOCK DATA',partial:false,lastUpdated:new Date(),exceptions:[]})};await TestBed.configureTestingModule({imports:[AppComponent],providers:[provideZonelessChangeDetection(),provideHttpClient(),{provide:CockpitFacade,useValue:facade}]}).compileComponents();const fixture=TestBed.createComponent(AppComponent);fixture.detectChanges();expect(fixture.nativeElement.textContent).toContain('Cardiologie');expect(fixture.componentInstance.state()).toBe('loaded');});});
